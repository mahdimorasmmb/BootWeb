import React from "react";

const LoginIcon = ({ className }) => {
  return (
    <>
      <i className={`fa-solid fa-arrow-right-to-bracket ${className}`}></i>
    </>
  );
};

export default LoginIcon;
